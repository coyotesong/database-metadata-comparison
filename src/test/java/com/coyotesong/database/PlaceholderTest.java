package com.coyotesong.pgxn;

import org.junit.jupiter.api.*;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * Placeholder test - should be removed once real tests exist.
 *
 * This class can be used to 1) verify that the tests will be run
 * and 2) that the logger is working.
 */
public class PlaceholderTest {
    private static final Logger LOG = LoggerFactory.getLogger(PlaceholderTest.class);

    @Test
    public void placeholder() {
        LOG.info("Running placeholder test");
        assert(true);
    }
}
